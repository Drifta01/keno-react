import kenoResults from "./hooks/kenoResults";

export default async function GetResults() {
  const params = {
    StartTs: "StartTs",
    FinishTs: "FinishTs",
    GameTypes: "GameTypes",
    Offset: "Offset",
    Count: "Count",
    lng: "lng",
  };
}
